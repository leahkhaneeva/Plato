<%= stylesheet_link_tag "posts" %>
<p id="notice"><%= notice %></p>

<% if user_signed_in? && current_user.isadmin == true %>
<div class="button_newpost">
<%= link_to 'Добавить', new_post_path %>
</div>
<% end %>
<%= react_component 'O_AllPosts_Cafes', { posts: @posts } %>
